name: Say Hello

on:
  push:
    branches: 
      - devs

jobs:
  hello_word:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola mundo!"
      - name: Dime la fecha
        run: date
      - run: ls -l